I'd rather name this directory `assets` than `extras`, because it takes a bit longer to run into distinction between `extras` and `examples` during CLI autocompletion. Still, I follow the Arduino library specification.
